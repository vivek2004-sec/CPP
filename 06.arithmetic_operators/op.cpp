#include <iostream>

int main() {
    using std::cout;
    int students = 20;
    // students += 1;
    // students++; #increment operator.

    // students -= 1;
    // students --;


    // students *= 1;

    // students /= 2;

    // double students /= 3;
    double remainder = students % 2;


    cout << students << "\n";
    cout << remainder << "\n";

    return 0;
}