#include <iostream>
#include <cmath>


int main () {
    using std::cout;
    using std::cin;

    int x = 3;
    int y = 55;
    int z;

    // z = std::max(x, y);

    // z = std::min(x, y);
    // z = pow(2, x);
    z = sqrt(9);

    cout << z;


    return 0;
}