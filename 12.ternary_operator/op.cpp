#include <iostream>

int main()
{

    // int grade = 70;
    // grade >= 45 ? std::printf("You've passed!") : std::printf("You've Failed.");

    // int number;
    // std::printf("Enter the number: ");
    // std::cin >> number;
    // number % 2 == 0 ? std::cout << "Even" : std::cout << "Odd";

    bool hungry = false;
    std::cout << (hungry ? "you are hungry" : "You are full.");
    return 0;
}